name: IRS Coherence

on:
  workflow_dispatch:
  schedule:
    - cron: "0 5 * * 1"   # co poniedziałek 05:00 UTC

# 👇 daje tokenowi prawo do zapisu w repo
permissions:
  contents: write

jobs:
  run-irs:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # ten token będzie miał uprawnienia zgodne z 'permissions:' powyżej
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install requirements
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run IRS Φ-coherence
        run: |
          python Sandbox/IRS/IRS_phi_coherence.py

      - name: Commit and push reports (if changed)
        shell: bash
        run: |
          git config user.name "innowatorium-bot"
          git config user.email "innowatorium-bot@users.noreply.github.com"

          # Dodaj bezpiecznie cały katalog raportów (nie wywali się gdy brak plików)
          git add -A Public/reports/

          # Commit tylko jeśli coś się zmieniło
          if ! git diff --cached --quiet; then
            git commit -m "Automatyczny raport IRS Φ-coherence [$(date +'%Y-%m-%d')]"
            git push
            echo "✅ Zmiany zacommitowane i wypchnięte."
          else
            echo "ℹ️ Brak zmian — nic do commitowania."
          fi


